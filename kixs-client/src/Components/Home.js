import Layout from "./Layout";

const Home = () => {
    return (
        <Layout> {/* the app-container element will be rendered in out in the Layout component */}
            <main className="app-container">
                Home
            </main>
        </Layout>
    )
}

export default Home;